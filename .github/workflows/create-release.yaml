name: "Create Release"

on:
  push:
    branches:
      - 'release-*'
  workflow_dispatch:
    inputs:
      name:
        description: 'Release name "2.1.3"'

jobs:
  run-something:
    name: Simple step
    runs-on: unutu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run code
        run: |
         pwd 
         ls -la

